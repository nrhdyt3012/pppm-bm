import Login from "./_components/login";

export const metadata = {
  title: "PPPM BM | Login",
  icons: {
    icon: "/logo_ppm.svg",
  },
};

export default function LoginPage() {
  return <Login />;
}
